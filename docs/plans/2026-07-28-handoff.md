# Session Handoff — 2026-07-28

_For the next session. Written after a session focused on: verifying the app runs, quick-win issue fixes via subagents, and two product brainstorms filed as issues._

---

## TL;DR / where things stand

- **App runs and is verified** against live Supabase (prior session confirmed egress fixed; auth, cook flow, chat handoff, all 5 themes all work end-to-end).
- **PR #74 (`feat/ui-overhaul` → main)** is the big stranded stack — design system + cook flow + the ready-for-agent queue. **Awaiting the user's sign-off. Do NOT merge without explicit approval.** Migration `00006` must be applied to prod Supabase when it lands.
- This session opened **5 small PRs** and filed **6 issues**. Nothing merged.

## Dev environment

- **Frontend runs on port 3100** this session (not 3000 — user's other project _ExperienceGeneration_ occupies 3000). Started with `cd nextjs && npm run dev -- -p 3100`.
- AI service on **8888**: `cd ai-service && uvicorn bubbly_chef.main:app --reload --port 8888`.
- **CORS**: `ai-service/.env` `BUBBLY_CORS_ORIGINS` was edited locally to include `http://localhost:3100`. That file is **gitignored (secrets)** — the change is local-only and not on any branch. If 3100 should be a durable default, add it to `ai-service/.env.example` (committed) too.
- Servers from this session may still be running in the background; a fresh session starts its own.
- Login: `test@bubbly.local` / `password`.

## GitHub / gh

- Repo is **`ayushb3/BubblyChef` (public)** on **github.com** — always prefix gh commands with `GH_HOST=github.com` (the machine also has a github.tools.sap SAP account that is the default host).

## Open PRs opened this session (all awaiting review — none merged)

| PR | Branch → base | Closes | Notes |
|---|---|---|---|
| **#136** | `fix/issue-130-tenacity-dep` → **main** | #130 | One line: `tenacity>=8.0` in `ai-service/pyproject.toml`. Independent of #74. |
| **#137** | `fix/issue-132-titlecase-names` → feat/ui-overhaul | #132 | `titleCase()` helper in `nextjs/src/lib/format.ts` + 9 jest cases; applied at 5 render sites. |
| **#141** | `fix/issue-133-yuzu-accent` → feat/ui-overhaul | #133 | Yuzu accent teal→forest green (`#A8D5A2` / `#4A8C45`), distinct from sakura. |
| **#142** | `fix/issue-110-131-category-tokens` → feat/ui-overhaul | #110, #131 | 10 `--color-cat-*` tokens; pantry `CATEGORY_BG` remapped off expiry/status tokens. |

Plus a docs branch **`docs/expiring-soon-brainstorm`** (pushed, no PR opened) holding `docs/plans/2026-07-28-expiring-soon-loop.md`.

### ⚠️ Subagent worktree gotcha (important for whoever reviews #141 / #142)
Two of the four subagents ran in git worktrees based on a **stale 40-line `globals.css`** (the real base is 132 lines with the 5-theme system). Their in-worktree edits were NOT trustworthy — a blind `git checkout <worktree-branch> -- globals.css` would have shipped the broken stale file. **The PM re-applied both changes by hand against the correct `origin/feat/ui-overhaul` base and tsc-verified each.** So #141 and #142 diffs are correct, but if you re-derive them from the worktree branches you'll get garbage. Trust the PR diffs, not `worktree-agent-*` branches. Those worktrees can be cleaned up (`git worktree prune` / remove `.claude/worktrees/agent-*`).

## Issues filed this session

**Perf/UX (filed, not started):**
- **#135** — dead-air after sign-in: no `loading.tsx` anywhere + all-or-nothing dashboard. Full root cause (3 sequential Supabase round-trips) + 2-part plan (add `loading.tsx`; stream `HeroHome` so greeting/mascot paint immediately since `displayName` is known server-side). User wants to spawn this from their phone later.

**"Expiring soon" rework** — user feels the feature is "lost": **not actionable** + **not proactive**. Design doc: `docs/plans/2026-07-28-expiring-soon-loop.md`.
- **#138** — deep-link expiring items into a seeded "cook this now" flow (the wedge; rides on #74's cook flow). _Do first._
- **#139** — dedicated "Use Soon" triage view (urgency-sorted, resolvable rows). Depends on #138 + #140.
- **#140** — resolve actions ("Used it up" / "Tossed it") + recordable outcomes (enables a future reward loop).
- **#43** (pre-existing) — cross-commented with the *proactive* half (morning nudge/digest, tiered urgency). Value gated by estimation accuracy (**#3**). Did NOT duplicate.

**Chat/UX:**
- **#143** — dashboard tip card opens an *empty* chat (`HeroHome.tsx:230` bare `<Link href="/chat">`). Should carry the tip in and have Bubbles explain why it works. Reuse the existing `?cooking=` param-context pattern in `nextjs/src/app/chat/page.tsx` (options A/B in the issue).

**Earlier this session (already filed):** #131 produce green, #132 lowercase names, #133 yuzu accent, #134 capture video demos (process note, not code).

## Key facts a new session needs (carried from prior handoff, still true)

- **Next.js is 16.2.2** (App Router, Turbopack) — NOT 14. Read `node_modules/next/dist/docs/` before writing Next code; `nextjs/AGENTS.md` warns APIs differ from training data.
- **Zustand is NOT a dependency.** Client state = React hooks/context; server state = React Query.
- `ruff` must stay pinned `<0.16` (newer default rules = 144 findings, #129). `mypy --strict` has 73 errors, not in CI (#128).
- Quality gates: `cd ai-service && pytest && ruff check bubbly_chef/` and `cd nextjs && npx tsc --noEmit`. Full `pytest` needs `BUBBLY_RUN_LIVE_TESTS=0` or it fails on live-provider tests (env, not code): with it off, 113 passed / 10 skipped.
- The 5-theme system (`sakura/mint/lavender/yuzu/bluebell`) lives in `nextjs/src/app/globals.css` as `[data-theme="*"]` blocks (132-line file on feat/ui-overhaul). Signal tokens (`--color-fresh/expiring/expired`) are intentionally theme-invariant.

## Suggested next steps (pick with the user)

1. Get the user's sign-off decision on **PR #74** (blocks the whole downstream queue since the cook flow only exists there).
2. Merge the independent small PRs once reviewed — **#136** (tenacity) can go to main now; #137/#141/#142 stack on #74.
3. Spawn **#135** (loading UX) and **#138** (seeded cook flow) — both high-leverage, both grounded specs.
4. Open a PR for the `docs/expiring-soon-brainstorm` branch if the doc should be tracked (or leave as reference branch).
5. `git worktree prune` to clean up the subagent worktrees under `.claude/worktrees/agent-*`.
